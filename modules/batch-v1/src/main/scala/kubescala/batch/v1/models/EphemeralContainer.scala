/** Kubernetes No description provided (generated by Openapi Generator
  * https://github.com/openapitools/openapi-generator)
  *
  * The version of the OpenAPI document: 1.34
  *
  * NOTE: This class is auto generated by OpenAPI Generator
  * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
  * the class manually.
  */
package kubescala.batch.v1.models

import com.github.plokhotnyuk.jsoniter_scala.macros.named

/** An EphemeralContainer is a temporary container that you may add to an
  * existing Pod for user-initiated activities such as debugging. Ephemeral
  * containers have no resource or scheduling guarantees, and they will not be
  * restarted when they exit or when a Pod is removed or restarted. The kubelet
  * may evict a Pod if an ephemeral container causes the Pod to exceed its
  * resource allocation. To add an ephemeral container, use the
  * ephemeralcontainers subresource of an existing Pod. Ephemeral containers may
  * not be removed or restarted.
  */
case class EphemeralContainer(
    /* Name of the ephemeral container specified as a DNS_LABEL. This name must be unique among all containers, init containers and ephemeral containers. */
    @named("name") name: String,
    /* Arguments to the entrypoint. The image's CMD is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container's environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. \"$$(VAR_NAME)\" will produce the string literal \"$(VAR_NAME)\". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell */
    @named("args") args: Option[Seq[String]] = scala.None,
    /* Entrypoint array. Not executed within a shell. The image's ENTRYPOINT is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container's environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. \"$$(VAR_NAME)\" will produce the string literal \"$(VAR_NAME)\". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell */
    @named("command") command: Option[Seq[String]] = scala.None,
    /* List of environment variables to set in the container. Cannot be updated. */
    @named("env") env: Option[Seq[EnvVar]] = scala.None,
    /* List of sources to populate environment variables in the container. The keys defined within a source may consist of any printable ASCII characters except '='. When a key exists in multiple sources, the value associated with the last source will take precedence. Values defined by an Env with a duplicate key will take precedence. Cannot be updated. */
    @named("envFrom") envFrom: Option[Seq[EnvFromSource]] = scala.None,
    /* Container image name. More info: https://kubernetes.io/docs/concepts/containers/images */
    @named("image") image: Option[String] = scala.None,
    /* Image pull policy. One of Always, Never, IfNotPresent. Defaults to Always if :latest tag is specified, or IfNotPresent otherwise. Cannot be updated. More info: https://kubernetes.io/docs/concepts/containers/images#updating-images  Possible enum values:  - `\"Always\"` means that kubelet always attempts to pull the latest image. Container will fail If the pull fails.  - `\"IfNotPresent\"` means that kubelet pulls if the image isn't present on disk. Container will fail if the image isn't present and the pull fails.  - `\"Never\"` means that kubelet never pulls an image, but only uses a local image. Container will fail if the image isn't present */
    @named("imagePullPolicy") imagePullPolicy: Option[
      EphemeralContainerEnums.ImagePullPolicy
    ] = scala.None,
    /* Lifecycle is not allowed for ephemeral containers. */
    @named("lifecycle") lifecycle: Option[Lifecycle] = scala.None,
    /* Probes are not allowed for ephemeral containers. */
    @named("livenessProbe") livenessProbe: Option[Probe] = scala.None,
    /* Ports are not allowed for ephemeral containers. */
    @named("ports") ports: Option[Seq[ContainerPort]] = scala.None,
    /* Probes are not allowed for ephemeral containers. */
    @named("readinessProbe") readinessProbe: Option[Probe] = scala.None,
    /* Resources resize policy for the container. */
    @named("resizePolicy") resizePolicy: Option[Seq[ContainerResizePolicy]] =
      scala.None,
    /* Resources are not allowed for ephemeral containers. Ephemeral containers use spare resources already allocated to the pod. */
    @named("resources") resources: Option[ResourceRequirements] = scala.None,
    /* Restart policy for the container to manage the restart behavior of each container within a pod. You cannot set this field on ephemeral containers. */
    @named("restartPolicy") restartPolicy: Option[String] = scala.None,
    /* Represents a list of rules to be checked to determine if the container should be restarted on exit. You cannot set this field on ephemeral containers. */
    @named("restartPolicyRules") restartPolicyRules: Option[
      Seq[ContainerRestartRule]
    ] = scala.None,
    /* Optional: SecurityContext defines the security options the ephemeral container should be run with. If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext. */
    @named("securityContext") securityContext: Option[SecurityContext] =
      scala.None,
    /* Probes are not allowed for ephemeral containers. */
    @named("startupProbe") startupProbe: Option[Probe] = scala.None,
    /* Whether this container should allocate a buffer for stdin in the container runtime. If this is not set, reads from stdin in the container will always result in EOF. Default is false. */
    @named("stdin") stdin: Option[Boolean] = scala.None,
    /* Whether the container runtime should close the stdin channel after it has been opened by a single attach. When stdin is true the stdin stream will remain open across multiple attach sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the first client attaches to stdin, and then remains open and accepts data until the client disconnects, at which time stdin is closed and remains closed until the container is restarted. If this flag is false, a container processes that reads from stdin will never receive an EOF. Default is false */
    @named("stdinOnce") stdinOnce: Option[Boolean] = scala.None,
    /* If set, the name of the container from PodSpec that this ephemeral container targets. The ephemeral container will be run in the namespaces (IPC, PID, etc) of this container. If not set then the ephemeral container uses the namespaces configured in the Pod spec.  The container runtime must implement support for this feature. If the runtime does not support namespace targeting then the result of setting this field is undefined. */
    @named("targetContainerName") targetContainerName: Option[String] =
      scala.None,
    /* Optional: Path at which the file to which the container's termination message will be written is mounted into the container's filesystem. Message written is intended to be brief final status, such as an assertion failure message. Will be truncated by the node if greater than 4096 bytes. The total message length across all containers will be limited to 12kb. Defaults to /dev/termination-log. Cannot be updated. */
    @named("terminationMessagePath") terminationMessagePath: Option[String] =
      scala.None,
    /* Indicate how the termination message should be populated. File will use the contents of terminationMessagePath to populate the container status message on both success and failure. FallbackToLogsOnError will use the last chunk of container log output if the termination message file is empty and the container exited with an error. The log output is limited to 2048 bytes or 80 lines, whichever is smaller. Defaults to File. Cannot be updated.  Possible enum values:  - `\"FallbackToLogsOnError\"` will read the most recent contents of the container logs for the container status message when the container exits with an error and the terminationMessagePath has no contents.  - `\"File\"` is the default behavior and will set the container status message to the contents of the container's terminationMessagePath when the container exits. */
    @named("terminationMessagePolicy") terminationMessagePolicy: Option[
      EphemeralContainerEnums.TerminationMessagePolicy
    ] = scala.None,
    /* Whether this container should allocate a TTY for itself, also requires 'stdin' to be true. Default is false. */
    @named("tty") tty: Option[Boolean] = scala.None,
    /* volumeDevices is the list of block devices to be used by the container. */
    @named("volumeDevices") volumeDevices: Option[Seq[VolumeDevice]] =
      scala.None,
    /* Pod volumes to mount into the container's filesystem. Subpath mounts are not allowed for ephemeral containers. Cannot be updated. */
    @named("volumeMounts") volumeMounts: Option[Seq[VolumeMount]] = scala.None,
    /* Container's working directory. If not specified, the container runtime's default will be used, which might be configured in the container image. Cannot be updated. */
    @named("workingDir") workingDir: Option[String] = scala.None
)

object EphemeralContainerEnums:
  enum ImagePullPolicy:
    case `Always`
    case `IfNotPresent`
    case `Never`

  object ImagePullPolicy:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given imagePullPolicyCodec: JsonValueCodec[ImagePullPolicy] =
      JsonCodecMaker.make {
        CodecMakerConfig
          .withAdtLeafClassNameMapper { x =>
            JsonCodecMaker.simpleClassName(x) match
              case "Always"       => "Always"
              case "IfNotPresent" => "IfNotPresent"
              case "Never"        => "Never"
          }
          .withDiscriminatorFieldName(scala.None)
      }
  enum TerminationMessagePolicy:
    case `FallbackToLogsOnError`
    case `File`

  object TerminationMessagePolicy:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given terminationMessagePolicyCodec
        : JsonValueCodec[TerminationMessagePolicy] = JsonCodecMaker.make {
      CodecMakerConfig
        .withAdtLeafClassNameMapper { x =>
          JsonCodecMaker.simpleClassName(x) match
            case "FallbackToLogsOnError" => "FallbackToLogsOnError"
            case "File"                  => "File"
        }
        .withDiscriminatorFieldName(scala.None)
    }
end EphemeralContainerEnums
