/** Kubernetes No description provided (generated by Openapi Generator
  * https://github.com/openapitools/openapi-generator)
  *
  * The version of the OpenAPI document: 1.34
  *
  * NOTE: This class is auto generated by OpenAPI Generator
  * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
  * the class manually.
  */
package kubescala.networking.v1.api

import kubescala.networking.v1.models.APIResourceList
import kubescala.networking.v1.models.DeleteOptions
import kubescala.networking.v1.models.IPAddress
import kubescala.networking.v1.models.IPAddressList
import kubescala.networking.v1.models.Ingress
import kubescala.networking.v1.models.IngressClass
import kubescala.networking.v1.models.IngressClassList
import kubescala.networking.v1.models.IngressList
import kubescala.networking.v1.models.NetworkPolicy
import kubescala.networking.v1.models.NetworkPolicyList
import kubescala.networking.v1.models.ServiceCIDR
import kubescala.networking.v1.models.ServiceCIDRList
import kubescala.networking.v1.models.Status
import kubescala.networking.v1.models.WatchEvent
import com.github.plokhotnyuk.jsoniter_scala.circe.JsoniterScalaCodec.*
import kubescala.networking.v1.JsonSupport.{*, given}
import kubescala.networking.v1.FormSerializable
import kubescala.networking.v1.FormStyleFormat
import kubescala.networking.v1.HeaderSerializable
import kubescala.networking.v1.ApiKeyLocation
import kubescala.networking.v1.PathStyleFormat
import kubescala.networking.v1.PathSerializable
import kubescala.networking.v1.CookieSerializable
import kubescala.networking.v1.Helpers.*
import sttp.client4.jsoniter.*
import sttp.client4.*
import sttp.model.Method

object NetworkingV1Api:
  def apply(baseUrl: String = "http://localhost"): NetworkingV1Api[
    kubescala.networking.v1.Authorization.NoAuthorization.type
  ] = NetworkingV1Api(
    baseUrl,
    kubescala.networking.v1.Authorization.NoAuthorization
  )
  def withBasicAuth(
      baseUrl: String,
      username: String,
      password: String
  ): NetworkingV1Api[kubescala.networking.v1.Authorization.BasicAuth] =
    NetworkingV1Api(
      baseUrl,
      kubescala.networking.v1.Authorization.BasicAuth(username, password)
    )

  def withApiKeyAuth(
      baseUrl: String,
      apiKey: String
  ): NetworkingV1Api[kubescala.networking.v1.Authorization.ApiKey] =
    NetworkingV1Api(
      baseUrl,
      kubescala.networking.v1.Authorization.ApiKey(apiKey)
    )

  def withBearerTokenAuth(
      baseUrl: String,
      token: String
  ): NetworkingV1Api[kubescala.networking.v1.Authorization.BearerToken] =
    NetworkingV1Api(
      baseUrl,
      kubescala.networking.v1.Authorization.BearerToken(token)
    )

case class NetworkingV1Api[
    Auth <: kubescala.networking.v1.Authorization
] private (baseUrl: String, authConfig: kubescala.networking.v1.Authorization):
  def withBasicAuth(
      username: String,
      password: String
  ): NetworkingV1Api[kubescala.networking.v1.Authorization.BasicAuth] =
    copy(authConfig =
      kubescala.networking.v1.Authorization.BasicAuth(username, password)
    )

  def withApiKeyAuth(
      apiKey: String
  ): NetworkingV1Api[kubescala.networking.v1.Authorization.ApiKey] =
    copy(authConfig = kubescala.networking.v1.Authorization.ApiKey(apiKey))

  def withNoAuth: NetworkingV1Api[
    kubescala.networking.v1.Authorization.NoAuthorization.type
  ] =
    copy(authConfig = kubescala.networking.v1.Authorization.NoAuthorization)

  def withBearerTokenAuth(
      token: String
  ): NetworkingV1Api[kubescala.networking.v1.Authorization.BearerToken] =
    copy(authConfig = kubescala.networking.v1.Authorization.BearerToken(token))

  /** create an IPAddress
    *
    * Expected answers: code 200 : IPAddress (OK) code 201 : IPAddress (Created)
    * code 202 : IPAddress (Accepted) code 401 : (Unauthorized)
    *
    * @param iPAddress
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createNetworkingV1IPAddress(
      iPAddress: IPAddress,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IPAddress]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ipaddresses"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(iPAddress))
      .response(asJson[IPAddress])

  /** create an IngressClass
    *
    * Expected answers: code 200 : IngressClass (OK) code 201 : IngressClass
    * (Created) code 202 : IngressClass (Accepted) code 401 : (Unauthorized)
    *
    * @param ingressClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createNetworkingV1IngressClass(
      ingressClass: IngressClass,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IngressClass]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ingressclasses"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(ingressClass))
      .response(asJson[IngressClass])

  /** create an Ingress
    *
    * Expected answers: code 200 : Ingress (OK) code 201 : Ingress (Created)
    * code 202 : Ingress (Accepted) code 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param ingress
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createNetworkingV1NamespacedIngress(
      namespace: String,
      ingress: Ingress,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Ingress]] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(ingress))
      .response(asJson[Ingress])

  /** create a NetworkPolicy
    *
    * Expected answers: code 200 : NetworkPolicy (OK) code 201 : NetworkPolicy
    * (Created) code 202 : NetworkPolicy (Accepted) code 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param networkPolicy
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createNetworkingV1NamespacedNetworkPolicy(
      namespace: String,
      networkPolicy: NetworkPolicy,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], NetworkPolicy]] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/networkpolicies"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(networkPolicy))
      .response(asJson[NetworkPolicy])

  /** create a ServiceCIDR
    *
    * Expected answers: code 200 : ServiceCIDR (OK) code 201 : ServiceCIDR
    * (Created) code 202 : ServiceCIDR (Accepted) code 401 : (Unauthorized)
    *
    * @param serviceCIDR
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createNetworkingV1ServiceCIDR(
      serviceCIDR: ServiceCIDR,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], ServiceCIDR]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(serviceCIDR))
      .response(asJson[ServiceCIDR])

  /** delete collection of IPAddress
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteNetworkingV1CollectionIPAddress(
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ipaddresses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete collection of IngressClass
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteNetworkingV1CollectionIngressClass(
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ingressclasses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete collection of Ingress
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteNetworkingV1CollectionNamespacedIngress(
      namespace: String,
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete collection of NetworkPolicy
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteNetworkingV1CollectionNamespacedNetworkPolicy(
      namespace: String,
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/networkpolicies"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete collection of ServiceCIDR
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteNetworkingV1CollectionServiceCIDR(
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete an IPAddress
    *
    * Expected answers: code 200 : Status (OK) code 202 : Status (Accepted) code
    * 401 : (Unauthorized)
    *
    * @param name
    *   name of the IPAddress
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteNetworkingV1IPAddress(
      name: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ipaddresses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete an IngressClass
    *
    * Expected answers: code 200 : Status (OK) code 202 : Status (Accepted) code
    * 401 : (Unauthorized)
    *
    * @param name
    *   name of the IngressClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteNetworkingV1IngressClass(
      name: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ingressclasses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete an Ingress
    *
    * Expected answers: code 200 : Status (OK) code 202 : Status (Accepted) code
    * 401 : (Unauthorized)
    *
    * @param name
    *   name of the Ingress
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteNetworkingV1NamespacedIngress(
      name: String,
      namespace: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete a NetworkPolicy
    *
    * Expected answers: code 200 : Status (OK) code 202 : Status (Accepted) code
    * 401 : (Unauthorized)
    *
    * @param name
    *   name of the NetworkPolicy
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteNetworkingV1NamespacedNetworkPolicy(
      name: String,
      namespace: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/networkpolicies/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete a ServiceCIDR
    *
    * Expected answers: code 200 : Status (OK) code 202 : Status (Accepted) code
    * 401 : (Unauthorized)
    *
    * @param name
    *   name of the ServiceCIDR
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteNetworkingV1ServiceCIDR(
      name: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** get available resources
    *
    * Expected answers: code 200 : APIResourceList (OK) code 401 :
    * (Unauthorized)
    */
  def getNetworkingV1APIResources: sttp.client4.Request[
    Either[ResponseException[String], APIResourceList]
  ] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/"

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[APIResourceList])

  /** list or watch objects of kind IPAddress
    *
    * Expected answers: code 200 : IPAddressList (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listNetworkingV1IPAddress(
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IPAddressList]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ipaddresses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[IPAddressList])

  /** list or watch objects of kind IngressClass
    *
    * Expected answers: code 200 : IngressClassList (OK) code 401 :
    * (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listNetworkingV1IngressClass(
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IngressClassList]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ingressclasses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[IngressClassList])

  /** list or watch objects of kind Ingress
    *
    * Expected answers: code 200 : IngressList (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listNetworkingV1IngressForAllNamespaces(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IngressList]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ingresses"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[IngressList])

  /** list or watch objects of kind Ingress
    *
    * Expected answers: code 200 : IngressList (OK) code 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listNetworkingV1NamespacedIngress(
      namespace: String,
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IngressList]] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[IngressList])

  /** list or watch objects of kind NetworkPolicy
    *
    * Expected answers: code 200 : NetworkPolicyList (OK) code 401 :
    * (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listNetworkingV1NamespacedNetworkPolicy(
      namespace: String,
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], NetworkPolicyList]
  ] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/networkpolicies"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[NetworkPolicyList])

  /** list or watch objects of kind NetworkPolicy
    *
    * Expected answers: code 200 : NetworkPolicyList (OK) code 401 :
    * (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listNetworkingV1NetworkPolicyForAllNamespaces(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], NetworkPolicyList]
  ] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/networkpolicies"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[NetworkPolicyList])

  /** list or watch objects of kind ServiceCIDR
    *
    * Expected answers: code 200 : ServiceCIDRList (OK) code 401 :
    * (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listNetworkingV1ServiceCIDR(
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], ServiceCIDRList]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[ServiceCIDRList])

  /** partially update the specified IPAddress
    *
    * Expected answers: code 200 : IPAddress (OK) code 201 : IPAddress (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the IPAddress
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchNetworkingV1IPAddress(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IPAddress]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ipaddresses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[IPAddress])

  /** partially update the specified IngressClass
    *
    * Expected answers: code 200 : IngressClass (OK) code 201 : IngressClass
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the IngressClass
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchNetworkingV1IngressClass(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IngressClass]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ingressclasses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[IngressClass])

  /** partially update the specified Ingress
    *
    * Expected answers: code 200 : Ingress (OK) code 201 : Ingress (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the Ingress
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchNetworkingV1NamespacedIngress(
      name: String,
      namespace: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Ingress]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[Ingress])

  /** partially update status of the specified Ingress
    *
    * Expected answers: code 200 : Ingress (OK) code 201 : Ingress (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the Ingress
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchNetworkingV1NamespacedIngressStatus(
      name: String,
      namespace: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Ingress]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses/${namePathParam}/status"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[Ingress])

  /** partially update the specified NetworkPolicy
    *
    * Expected answers: code 200 : NetworkPolicy (OK) code 201 : NetworkPolicy
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the NetworkPolicy
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchNetworkingV1NamespacedNetworkPolicy(
      name: String,
      namespace: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], NetworkPolicy]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/networkpolicies/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[NetworkPolicy])

  /** partially update the specified ServiceCIDR
    *
    * Expected answers: code 200 : ServiceCIDR (OK) code 201 : ServiceCIDR
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the ServiceCIDR
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchNetworkingV1ServiceCIDR(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], ServiceCIDR]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[ServiceCIDR])

  /** partially update status of the specified ServiceCIDR
    *
    * Expected answers: code 200 : ServiceCIDR (OK) code 201 : ServiceCIDR
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the ServiceCIDR
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchNetworkingV1ServiceCIDRStatus(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], ServiceCIDR]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs/${namePathParam}/status"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[ServiceCIDR])

  /** read the specified IPAddress
    *
    * Expected answers: code 200 : IPAddress (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the IPAddress
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readNetworkingV1IPAddress(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IPAddress]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ipaddresses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[IPAddress])

  /** read the specified IngressClass
    *
    * Expected answers: code 200 : IngressClass (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the IngressClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readNetworkingV1IngressClass(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IngressClass]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ingressclasses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[IngressClass])

  /** read the specified Ingress
    *
    * Expected answers: code 200 : Ingress (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the Ingress
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readNetworkingV1NamespacedIngress(
      name: String,
      namespace: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Ingress]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[Ingress])

  /** read status of the specified Ingress
    *
    * Expected answers: code 200 : Ingress (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the Ingress
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readNetworkingV1NamespacedIngressStatus(
      name: String,
      namespace: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Ingress]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses/${namePathParam}/status"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[Ingress])

  /** read the specified NetworkPolicy
    *
    * Expected answers: code 200 : NetworkPolicy (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the NetworkPolicy
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readNetworkingV1NamespacedNetworkPolicy(
      name: String,
      namespace: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], NetworkPolicy]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/networkpolicies/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[NetworkPolicy])

  /** read the specified ServiceCIDR
    *
    * Expected answers: code 200 : ServiceCIDR (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the ServiceCIDR
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readNetworkingV1ServiceCIDR(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], ServiceCIDR]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[ServiceCIDR])

  /** read status of the specified ServiceCIDR
    *
    * Expected answers: code 200 : ServiceCIDR (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the ServiceCIDR
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readNetworkingV1ServiceCIDRStatus(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], ServiceCIDR]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs/${namePathParam}/status"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[ServiceCIDR])

  /** replace the specified IPAddress
    *
    * Expected answers: code 200 : IPAddress (OK) code 201 : IPAddress (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the IPAddress
    * @param iPAddress
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceNetworkingV1IPAddress(
      name: String,
      iPAddress: IPAddress,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IPAddress]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ipaddresses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(iPAddress))
      .response(asJson[IPAddress])

  /** replace the specified IngressClass
    *
    * Expected answers: code 200 : IngressClass (OK) code 201 : IngressClass
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the IngressClass
    * @param ingressClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceNetworkingV1IngressClass(
      name: String,
      ingressClass: IngressClass,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], IngressClass]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/ingressclasses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(ingressClass))
      .response(asJson[IngressClass])

  /** replace the specified Ingress
    *
    * Expected answers: code 200 : Ingress (OK) code 201 : Ingress (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the Ingress
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param ingress
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceNetworkingV1NamespacedIngress(
      name: String,
      namespace: String,
      ingress: Ingress,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Ingress]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(ingress))
      .response(asJson[Ingress])

  /** replace status of the specified Ingress
    *
    * Expected answers: code 200 : Ingress (OK) code 201 : Ingress (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the Ingress
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param ingress
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceNetworkingV1NamespacedIngressStatus(
      name: String,
      namespace: String,
      ingress: Ingress,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Ingress]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/ingresses/${namePathParam}/status"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(ingress))
      .response(asJson[Ingress])

  /** replace the specified NetworkPolicy
    *
    * Expected answers: code 200 : NetworkPolicy (OK) code 201 : NetworkPolicy
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the NetworkPolicy
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param networkPolicy
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceNetworkingV1NamespacedNetworkPolicy(
      name: String,
      namespace: String,
      networkPolicy: NetworkPolicy,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], NetworkPolicy]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/namespaces/${namespacePathParam}/networkpolicies/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(networkPolicy))
      .response(asJson[NetworkPolicy])

  /** replace the specified ServiceCIDR
    *
    * Expected answers: code 200 : ServiceCIDR (OK) code 201 : ServiceCIDR
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the ServiceCIDR
    * @param serviceCIDR
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceNetworkingV1ServiceCIDR(
      name: String,
      serviceCIDR: ServiceCIDR,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], ServiceCIDR]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(serviceCIDR))
      .response(asJson[ServiceCIDR])

  /** replace status of the specified ServiceCIDR
    *
    * Expected answers: code 200 : ServiceCIDR (OK) code 201 : ServiceCIDR
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the ServiceCIDR
    * @param serviceCIDR
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceNetworkingV1ServiceCIDRStatus(
      name: String,
      serviceCIDR: ServiceCIDR,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], ServiceCIDR]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/servicecidrs/${namePathParam}/status"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(serviceCIDR))
      .response(asJson[ServiceCIDR])

  /** watch changes to an object of kind IPAddress. deprecated: use the 'watch'
    * parameter with a list operation instead, filtered to a single item with
    * the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the IPAddress
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1IPAddress(
      name: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/ipaddresses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of IPAddress. deprecated: use the
    * 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1IPAddressList(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/ipaddresses"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch changes to an object of kind IngressClass. deprecated: use the
    * 'watch' parameter with a list operation instead, filtered to a single item
    * with the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the IngressClass
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1IngressClass(
      name: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/ingressclasses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of IngressClass. deprecated: use the
    * 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1IngressClassList(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/ingressclasses"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of Ingress. deprecated: use the 'watch'
    * parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1IngressListForAllNamespaces(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/ingresses"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch changes to an object of kind Ingress. deprecated: use the 'watch'
    * parameter with a list operation instead, filtered to a single item with
    * the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the Ingress
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1NamespacedIngress(
      name: String,
      namespace: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/namespaces/${namespacePathParam}/ingresses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of Ingress. deprecated: use the 'watch'
    * parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1NamespacedIngressList(
      namespace: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/namespaces/${namespacePathParam}/ingresses"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch changes to an object of kind NetworkPolicy. deprecated: use the
    * 'watch' parameter with a list operation instead, filtered to a single item
    * with the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the NetworkPolicy
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1NamespacedNetworkPolicy(
      name: String,
      namespace: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/namespaces/${namespacePathParam}/networkpolicies/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of NetworkPolicy. deprecated: use the
    * 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1NamespacedNetworkPolicyList(
      namespace: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/namespaces/${namespacePathParam}/networkpolicies"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of NetworkPolicy. deprecated: use the
    * 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1NetworkPolicyListForAllNamespaces(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/networkpolicies"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch changes to an object of kind ServiceCIDR. deprecated: use the
    * 'watch' parameter with a list operation instead, filtered to a single item
    * with the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the ServiceCIDR
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1ServiceCIDR(
      name: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/servicecidrs/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of ServiceCIDR. deprecated: use the
    * 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchNetworkingV1ServiceCIDRList(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/networking.k8s.io/v1/watch/servicecidrs"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

end NetworkingV1Api
