/**
 * Kubernetes
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.34
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package kubescala.api.v1.models

import com.github.plokhotnyuk.jsoniter_scala.macros.named

  /**
   * NodeStatus is information about the current status of a node.
   */
case class NodeStatus(
  /* List of addresses reachable to the node. Queried from cloud provider, if available. More info: https://kubernetes.io/docs/reference/node/node-status/#addresses Note: This field is declared as mergeable, but the merge key is not sufficiently unique, which can cause data corruption when it is merged. Callers should instead use a full-replacement patch. See https://pr.k8s.io/79391 for an example. Consumers should assume that addresses can change during the lifetime of a Node. However, there are some exceptions where this may not be possible, such as Pods that inherit a Node's address in its own status or consumers of the downward API (status.hostIP). */
  @named("addresses") addresses: Option[Seq[NodeAddress]] = scala.None,
  /* Allocatable represents the resources of a node that are available for scheduling. Defaults to Capacity. */
  @named("allocatable") allocatable: Option[Map[String, Quantity]] = scala.None,
  /* Capacity represents the total resources of a node. More info: https://kubernetes.io/docs/reference/node/node-status/#capacity */
  @named("capacity") capacity: Option[Map[String, Quantity]] = scala.None,
  /* Conditions is an array of current observed node conditions. More info: https://kubernetes.io/docs/reference/node/node-status/#condition */
  @named("conditions") conditions: Option[Seq[NodeCondition]] = scala.None,
  /* Status of the config assigned to the node via the dynamic Kubelet config feature. */
  @named("config") config: Option[NodeConfigStatus] = scala.None,
  /* Endpoints of daemons running on the Node. */
  @named("daemonEndpoints") daemonEndpoints: Option[NodeDaemonEndpoints] = scala.None,
  /* Features describes the set of features implemented by the CRI implementation. */
  @named("features") features: Option[NodeFeatures] = scala.None,
  /* List of container images on this node */
  @named("images") images: Option[Seq[ContainerImage]] = scala.None,
  /* Set of ids/uuids to uniquely identify the node. More info: https://kubernetes.io/docs/reference/node/node-status/#info */
  @named("nodeInfo") nodeInfo: Option[NodeSystemInfo] = scala.None,
  /* NodePhase is the recently observed lifecycle phase of the node. More info: https://kubernetes.io/docs/concepts/nodes/node/#phase The field is never populated, and now is deprecated.  Possible enum values:  - `\"Pending\"` means the node has been created/added by the system, but not configured.  - `\"Running\"` means the node has been configured and has Kubernetes components running.  - `\"Terminated\"` means the node has been removed from the cluster. */
  @named("phase") phase: Option[NodeStatusEnums.Phase] = scala.None,
  /* The available runtime handlers. */
  @named("runtimeHandlers") runtimeHandlers: Option[Seq[NodeRuntimeHandler]] = scala.None,
  /* List of volumes that are attached to the node. */
  @named("volumesAttached") volumesAttached: Option[Seq[AttachedVolume]] = scala.None,
  /* List of attachable volumes in use (mounted) by the node. */
  @named("volumesInUse") volumesInUse: Option[Seq[String]] = scala.None
)

object NodeStatusEnums:
  enum Phase:
    case `Pending`
    case `Running`
    case `Terminated`

  object Phase:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given phaseCodec: JsonValueCodec[Phase] = JsonCodecMaker.make {
      CodecMakerConfig
        .withAdtLeafClassNameMapper { x =>
          JsonCodecMaker.simpleClassName(x) match
            case "Pending" => "Pending"
            case "Running" => "Running"
            case "Terminated" => "Terminated"
        }
        .withDiscriminatorFieldName(scala.None)
    }
end NodeStatusEnums
