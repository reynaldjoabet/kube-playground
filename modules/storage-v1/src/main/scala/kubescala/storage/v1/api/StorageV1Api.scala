/** Kubernetes No description provided (generated by Openapi Generator
  * https://github.com/openapitools/openapi-generator)
  *
  * The version of the OpenAPI document: 1.34
  *
  * NOTE: This class is auto generated by OpenAPI Generator
  * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
  * the class manually.
  */
package kubescala.storage.v1.api

import kubescala.storage.v1.models.APIResourceList
import kubescala.storage.v1.models.CSIDriver
import kubescala.storage.v1.models.CSIDriverList
import kubescala.storage.v1.models.CSINode
import kubescala.storage.v1.models.CSINodeList
import kubescala.storage.v1.models.CSIStorageCapacity
import kubescala.storage.v1.models.CSIStorageCapacityList
import kubescala.storage.v1.models.DeleteOptions
import kubescala.storage.v1.models.Status
import kubescala.storage.v1.models.StorageClass
import kubescala.storage.v1.models.StorageClassList
import kubescala.storage.v1.models.VolumeAttachment
import kubescala.storage.v1.models.VolumeAttachmentList
import kubescala.storage.v1.models.VolumeAttributesClass
import kubescala.storage.v1.models.VolumeAttributesClassList
import kubescala.storage.v1.models.WatchEvent
import com.github.plokhotnyuk.jsoniter_scala.circe.JsoniterScalaCodec.*
import kubescala.storage.v1.JsonSupport.{*, given}
import kubescala.storage.v1.FormSerializable
import kubescala.storage.v1.FormStyleFormat
import kubescala.storage.v1.HeaderSerializable
import kubescala.storage.v1.ApiKeyLocation
import kubescala.storage.v1.PathStyleFormat
import kubescala.storage.v1.PathSerializable
import kubescala.storage.v1.CookieSerializable
import kubescala.storage.v1.Helpers.*
import sttp.client4.jsoniter.*
import sttp.client4.*
import sttp.model.Method

object StorageV1Api:
  def apply(
      baseUrl: String = "http://localhost"
  ): StorageV1Api[kubescala.storage.v1.Authorization.NoAuthorization.type] =
    StorageV1Api(baseUrl, kubescala.storage.v1.Authorization.NoAuthorization)
  def withBasicAuth(
      baseUrl: String,
      username: String,
      password: String
  ): StorageV1Api[kubescala.storage.v1.Authorization.BasicAuth] =
    StorageV1Api(
      baseUrl,
      kubescala.storage.v1.Authorization.BasicAuth(username, password)
    )

  def withApiKeyAuth(
      baseUrl: String,
      apiKey: String
  ): StorageV1Api[kubescala.storage.v1.Authorization.ApiKey] =
    StorageV1Api(baseUrl, kubescala.storage.v1.Authorization.ApiKey(apiKey))

  def withBearerTokenAuth(
      baseUrl: String,
      token: String
  ): StorageV1Api[kubescala.storage.v1.Authorization.BearerToken] =
    StorageV1Api(baseUrl, kubescala.storage.v1.Authorization.BearerToken(token))

case class StorageV1Api[Auth <: kubescala.storage.v1.Authorization] private (
    baseUrl: String,
    authConfig: kubescala.storage.v1.Authorization
):
  def withBasicAuth(
      username: String,
      password: String
  ): StorageV1Api[kubescala.storage.v1.Authorization.BasicAuth] =
    copy(authConfig =
      kubescala.storage.v1.Authorization.BasicAuth(username, password)
    )

  def withApiKeyAuth(
      apiKey: String
  ): StorageV1Api[kubescala.storage.v1.Authorization.ApiKey] =
    copy(authConfig = kubescala.storage.v1.Authorization.ApiKey(apiKey))

  def withNoAuth
      : StorageV1Api[kubescala.storage.v1.Authorization.NoAuthorization.type] =
    copy(authConfig = kubescala.storage.v1.Authorization.NoAuthorization)

  def withBearerTokenAuth(
      token: String
  ): StorageV1Api[kubescala.storage.v1.Authorization.BearerToken] =
    copy(authConfig = kubescala.storage.v1.Authorization.BearerToken(token))

  /** create a CSIDriver
    *
    * Expected answers: code 200 : CSIDriver (OK) code 201 : CSIDriver (Created)
    * code 202 : CSIDriver (Accepted) code 401 : (Unauthorized)
    *
    * @param cSIDriver
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createStorageV1CSIDriver(
      cSIDriver: CSIDriver,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSIDriver]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csidrivers"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(cSIDriver))
      .response(asJson[CSIDriver])

  /** create a CSINode
    *
    * Expected answers: code 200 : CSINode (OK) code 201 : CSINode (Created)
    * code 202 : CSINode (Accepted) code 401 : (Unauthorized)
    *
    * @param cSINode
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createStorageV1CSINode(
      cSINode: CSINode,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSINode]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csinodes"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(cSINode))
      .response(asJson[CSINode])

  /** create a CSIStorageCapacity
    *
    * Expected answers: code 200 : CSIStorageCapacity (OK) code 201 :
    * CSIStorageCapacity (Created) code 202 : CSIStorageCapacity (Accepted) code
    * 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param cSIStorageCapacity
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createStorageV1NamespacedCSIStorageCapacity(
      namespace: String,
      cSIStorageCapacity: CSIStorageCapacity,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], CSIStorageCapacity]
  ] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/namespaces/${namespacePathParam}/csistoragecapacities"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(cSIStorageCapacity))
      .response(asJson[CSIStorageCapacity])

  /** create a StorageClass
    *
    * Expected answers: code 200 : StorageClass (OK) code 201 : StorageClass
    * (Created) code 202 : StorageClass (Accepted) code 401 : (Unauthorized)
    *
    * @param storageClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createStorageV1StorageClass(
      storageClass: StorageClass,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], StorageClass]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/storageclasses"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(storageClass))
      .response(asJson[StorageClass])

  /** create a VolumeAttachment
    *
    * Expected answers: code 200 : VolumeAttachment (OK) code 201 :
    * VolumeAttachment (Created) code 202 : VolumeAttachment (Accepted) code 401
    * : (Unauthorized)
    *
    * @param volumeAttachment
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createStorageV1VolumeAttachment(
      volumeAttachment: VolumeAttachment,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], VolumeAttachment]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(volumeAttachment))
      .response(asJson[VolumeAttachment])

  /** create a VolumeAttributesClass
    *
    * Expected answers: code 200 : VolumeAttributesClass (OK) code 201 :
    * VolumeAttributesClass (Created) code 202 : VolumeAttributesClass
    * (Accepted) code 401 : (Unauthorized)
    *
    * @param volumeAttributesClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def createStorageV1VolumeAttributesClass(
      volumeAttributesClass: VolumeAttributesClass,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], VolumeAttributesClass]
  ] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattributesclasses"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.POST, requestURL)
      .contentType("application/json")
      .body(asJson(volumeAttributesClass))
      .response(asJson[VolumeAttributesClass])

  /** delete a CSIDriver
    *
    * Expected answers: code 200 : CSIDriver (OK) code 202 : CSIDriver
    * (Accepted) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSIDriver
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteStorageV1CSIDriver(
      name: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSIDriver]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csidrivers/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[CSIDriver])

  /** delete a CSINode
    *
    * Expected answers: code 200 : CSINode (OK) code 202 : CSINode (Accepted)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSINode
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteStorageV1CSINode(
      name: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSINode]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csinodes/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[CSINode])

  /** delete collection of CSIDriver
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteStorageV1CollectionCSIDriver(
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csidrivers"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete collection of CSINode
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteStorageV1CollectionCSINode(
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csinodes"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete collection of CSIStorageCapacity
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteStorageV1CollectionNamespacedCSIStorageCapacity(
      namespace: String,
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/namespaces/${namespacePathParam}/csistoragecapacities"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete collection of StorageClass
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteStorageV1CollectionStorageClass(
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/storageclasses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete collection of VolumeAttachment
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteStorageV1CollectionVolumeAttachment(
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete collection of VolumeAttributesClass
    *
    * Expected answers: code 200 : Status (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param deleteOptions
    */
  def deleteStorageV1CollectionVolumeAttributesClass(
      pretty: Option[String] = scala.None,
      continue: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattributesclasses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "dryRun",
            dryRun,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete a CSIStorageCapacity
    *
    * Expected answers: code 200 : Status (OK) code 202 : Status (Accepted) code
    * 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSIStorageCapacity
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteStorageV1NamespacedCSIStorageCapacity(
      name: String,
      namespace: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], Status]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/namespaces/${namespacePathParam}/csistoragecapacities/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[Status])

  /** delete a StorageClass
    *
    * Expected answers: code 200 : StorageClass (OK) code 202 : StorageClass
    * (Accepted) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the StorageClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteStorageV1StorageClass(
      name: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], StorageClass]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/storageclasses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[StorageClass])

  /** delete a VolumeAttachment
    *
    * Expected answers: code 200 : VolumeAttachment (OK) code 202 :
    * VolumeAttachment (Accepted) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttachment
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteStorageV1VolumeAttachment(
      name: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], VolumeAttachment]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[VolumeAttachment])

  /** delete a VolumeAttributesClass
    *
    * Expected answers: code 200 : VolumeAttributesClass (OK) code 202 :
    * VolumeAttributesClass (Accepted) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttributesClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param gracePeriodSeconds
    *   The duration in seconds before the object should be deleted. Value must
    *   be non-negative integer. The value zero indicates delete immediately. If
    *   this value is nil, the default grace period for the specified type will
    *   be used. Defaults to a per object value if not specified. zero means
    *   delete immediately.
    * @param ignoreStoreReadErrorWithClusterBreakingPotential
    *   if set to true, it will trigger an unsafe deletion of the resource in
    *   case the normal deletion flow fails with a corrupt object error. A
    *   resource is considered corrupt if it can not be retrieved from the
    *   underlying storage successfully because of a) its data can not be
    *   transformed e.g. decryption failure, or b) it fails to decode into an
    *   object. NOTE: unsafe deletion ignores finalizer constraints, skips
    *   precondition checks, and removes the object from the storage. WARNING:
    *   This may potentially break the cluster if the workload associated with
    *   the resource being unsafe-deleted relies on normal deletion flow. Use
    *   only if you REALLY know what you are doing. The default value is false,
    *   and the user must opt in to enable it
    * @param orphanDependents
    *   Deprecated: please use the PropagationPolicy, this field will be
    *   deprecated in 1.7. Should the dependent objects be orphaned. If
    *   true/false, the \"orphan\" finalizer will be added to/removed from the
    *   object's finalizers list. Either this field or PropagationPolicy may be
    *   set, but not both.
    * @param propagationPolicy
    *   Whether and how garbage collection will be performed. Either this field
    *   or OrphanDependents may be set, but not both. The default policy is
    *   decided by the existing finalizer set in the metadata.finalizers and the
    *   resource-specific default policy. Acceptable values are: 'Orphan' -
    *   orphan the dependents; 'Background' - allow the garbage collector to
    *   delete the dependents in the background; 'Foreground' - a cascading
    *   policy that deletes all dependents in the foreground.
    * @param deleteOptions
    */
  def deleteStorageV1VolumeAttributesClass(
      name: String,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      gracePeriodSeconds: Option[Int] = scala.None,
      ignoreStoreReadErrorWithClusterBreakingPotential: Option[Boolean] =
        scala.None,
      orphanDependents: Option[Boolean] = scala.None,
      propagationPolicy: Option[String] = scala.None,
      deleteOptions: Option[DeleteOptions] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], VolumeAttributesClass]
  ] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattributesclasses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "gracePeriodSeconds",
            gracePeriodSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "ignoreStoreReadErrorWithClusterBreakingPotential",
            ignoreStoreReadErrorWithClusterBreakingPotential,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "orphanDependents",
            orphanDependents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "propagationPolicy",
            propagationPolicy,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.DELETE, requestURL)
      .contentType("application/json")
      .body(asJson(deleteOptions))
      .response(asJson[VolumeAttributesClass])

  /** get available resources
    *
    * Expected answers: code 200 : APIResourceList (OK) code 401 :
    * (Unauthorized)
    */
  def getStorageV1APIResources: sttp.client4.Request[
    Either[ResponseException[String], APIResourceList]
  ] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/"

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[APIResourceList])

  /** list or watch objects of kind CSIDriver
    *
    * Expected answers: code 200 : CSIDriverList (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listStorageV1CSIDriver(
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSIDriverList]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csidrivers"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[CSIDriverList])

  /** list or watch objects of kind CSINode
    *
    * Expected answers: code 200 : CSINodeList (OK) code 401 : (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listStorageV1CSINode(
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSINodeList]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csinodes"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[CSINodeList])

  /** list or watch objects of kind CSIStorageCapacity
    *
    * Expected answers: code 200 : CSIStorageCapacityList (OK) code 401 :
    * (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listStorageV1CSIStorageCapacityForAllNamespaces(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], CSIStorageCapacityList]
  ] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csistoragecapacities"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[CSIStorageCapacityList])

  /** list or watch objects of kind CSIStorageCapacity
    *
    * Expected answers: code 200 : CSIStorageCapacityList (OK) code 401 :
    * (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listStorageV1NamespacedCSIStorageCapacity(
      namespace: String,
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], CSIStorageCapacityList]
  ] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/namespaces/${namespacePathParam}/csistoragecapacities"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[CSIStorageCapacityList])

  /** list or watch objects of kind StorageClass
    *
    * Expected answers: code 200 : StorageClassList (OK) code 401 :
    * (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listStorageV1StorageClass(
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], StorageClassList]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/storageclasses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[StorageClassList])

  /** list or watch objects of kind VolumeAttachment
    *
    * Expected answers: code 200 : VolumeAttachmentList (OK) code 401 :
    * (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listStorageV1VolumeAttachment(
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], VolumeAttachmentList]
  ] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[VolumeAttachmentList])

  /** list or watch objects of kind VolumeAttributesClass
    *
    * Expected answers: code 200 : VolumeAttributesClassList (OK) code 401 :
    * (Unauthorized)
    *
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def listStorageV1VolumeAttributesClass(
      pretty: Option[String] = scala.None,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], VolumeAttributesClassList]
  ] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattributesclasses"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("continue", continue, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[VolumeAttributesClassList])

  /** partially update the specified CSIDriver
    *
    * Expected answers: code 200 : CSIDriver (OK) code 201 : CSIDriver (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSIDriver
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchStorageV1CSIDriver(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSIDriver]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csidrivers/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[CSIDriver])

  /** partially update the specified CSINode
    *
    * Expected answers: code 200 : CSINode (OK) code 201 : CSINode (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSINode
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchStorageV1CSINode(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSINode]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csinodes/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[CSINode])

  /** partially update the specified CSIStorageCapacity
    *
    * Expected answers: code 200 : CSIStorageCapacity (OK) code 201 :
    * CSIStorageCapacity (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSIStorageCapacity
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchStorageV1NamespacedCSIStorageCapacity(
      name: String,
      namespace: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], CSIStorageCapacity]
  ] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/namespaces/${namespacePathParam}/csistoragecapacities/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[CSIStorageCapacity])

  /** partially update the specified StorageClass
    *
    * Expected answers: code 200 : StorageClass (OK) code 201 : StorageClass
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the StorageClass
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchStorageV1StorageClass(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], StorageClass]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/storageclasses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[StorageClass])

  /** partially update the specified VolumeAttachment
    *
    * Expected answers: code 200 : VolumeAttachment (OK) code 201 :
    * VolumeAttachment (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttachment
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchStorageV1VolumeAttachment(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], VolumeAttachment]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[VolumeAttachment])

  /** partially update status of the specified VolumeAttachment
    *
    * Expected answers: code 200 : VolumeAttachment (OK) code 201 :
    * VolumeAttachment (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttachment
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchStorageV1VolumeAttachmentStatus(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], VolumeAttachment]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments/${namePathParam}/status"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[VolumeAttachment])

  /** partially update the specified VolumeAttributesClass
    *
    * Expected answers: code 200 : VolumeAttributesClass (OK) code 201 :
    * VolumeAttributesClass (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttributesClass
    * @param body
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint. This field is required for
    *   apply requests (application/apply-patch) but optional for non-apply
    *   patch types (JsonPatch, MergePatch, StrategicMergePatch).
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    * @param force
    *   Force is going to \"force\" Apply requests. It means user will
    *   re-acquire conflicting fields owned by other people. Force flag must be
    *   unset for non-apply patch requests.
    */
  def patchStorageV1VolumeAttributesClass(
      name: String,
      body: io.circe.Json,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None,
      force: Option[Boolean] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], VolumeAttributesClass]
  ] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattributesclasses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("force", force, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.PATCH, requestURL)
      .contentType("application/apply-patch+yaml")
      .body(asJson(body))
      .response(asJson[VolumeAttributesClass])

  /** read the specified CSIDriver
    *
    * Expected answers: code 200 : CSIDriver (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSIDriver
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readStorageV1CSIDriver(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSIDriver]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csidrivers/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[CSIDriver])

  /** read the specified CSINode
    *
    * Expected answers: code 200 : CSINode (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSINode
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readStorageV1CSINode(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSINode]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csinodes/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[CSINode])

  /** read the specified CSIStorageCapacity
    *
    * Expected answers: code 200 : CSIStorageCapacity (OK) code 401 :
    * (Unauthorized)
    *
    * @param name
    *   name of the CSIStorageCapacity
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readStorageV1NamespacedCSIStorageCapacity(
      name: String,
      namespace: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], CSIStorageCapacity]
  ] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/namespaces/${namespacePathParam}/csistoragecapacities/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[CSIStorageCapacity])

  /** read the specified StorageClass
    *
    * Expected answers: code 200 : StorageClass (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the StorageClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readStorageV1StorageClass(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], StorageClass]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/storageclasses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[StorageClass])

  /** read the specified VolumeAttachment
    *
    * Expected answers: code 200 : VolumeAttachment (OK) code 401 :
    * (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttachment
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readStorageV1VolumeAttachment(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], VolumeAttachment]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[VolumeAttachment])

  /** read status of the specified VolumeAttachment
    *
    * Expected answers: code 200 : VolumeAttachment (OK) code 401 :
    * (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttachment
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readStorageV1VolumeAttachmentStatus(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], VolumeAttachment]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments/${namePathParam}/status"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[VolumeAttachment])

  /** read the specified VolumeAttributesClass
    *
    * Expected answers: code 200 : VolumeAttributesClass (OK) code 401 :
    * (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttributesClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    */
  def readStorageV1VolumeAttributesClass(
      name: String,
      pretty: Option[String] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], VolumeAttributesClass]
  ] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattributesclasses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "pretty",
            pretty,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[VolumeAttributesClass])

  /** replace the specified CSIDriver
    *
    * Expected answers: code 200 : CSIDriver (OK) code 201 : CSIDriver (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSIDriver
    * @param cSIDriver
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceStorageV1CSIDriver(
      name: String,
      cSIDriver: CSIDriver,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSIDriver]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csidrivers/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(cSIDriver))
      .response(asJson[CSIDriver])

  /** replace the specified CSINode
    *
    * Expected answers: code 200 : CSINode (OK) code 201 : CSINode (Created)
    * code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSINode
    * @param cSINode
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceStorageV1CSINode(
      name: String,
      cSINode: CSINode,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], CSINode]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/csinodes/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(cSINode))
      .response(asJson[CSINode])

  /** replace the specified CSIStorageCapacity
    *
    * Expected answers: code 200 : CSIStorageCapacity (OK) code 201 :
    * CSIStorageCapacity (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSIStorageCapacity
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param cSIStorageCapacity
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceStorageV1NamespacedCSIStorageCapacity(
      name: String,
      namespace: String,
      cSIStorageCapacity: CSIStorageCapacity,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], CSIStorageCapacity]
  ] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/namespaces/${namespacePathParam}/csistoragecapacities/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(cSIStorageCapacity))
      .response(asJson[CSIStorageCapacity])

  /** replace the specified StorageClass
    *
    * Expected answers: code 200 : StorageClass (OK) code 201 : StorageClass
    * (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the StorageClass
    * @param storageClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceStorageV1StorageClass(
      name: String,
      storageClass: StorageClass,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], StorageClass]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/storageclasses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(storageClass))
      .response(asJson[StorageClass])

  /** replace the specified VolumeAttachment
    *
    * Expected answers: code 200 : VolumeAttachment (OK) code 201 :
    * VolumeAttachment (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttachment
    * @param volumeAttachment
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceStorageV1VolumeAttachment(
      name: String,
      volumeAttachment: VolumeAttachment,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], VolumeAttachment]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(volumeAttachment))
      .response(asJson[VolumeAttachment])

  /** replace status of the specified VolumeAttachment
    *
    * Expected answers: code 200 : VolumeAttachment (OK) code 201 :
    * VolumeAttachment (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttachment
    * @param volumeAttachment
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceStorageV1VolumeAttachmentStatus(
      name: String,
      volumeAttachment: VolumeAttachment,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], VolumeAttachment]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattachments/${namePathParam}/status"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(volumeAttachment))
      .response(asJson[VolumeAttachment])

  /** replace the specified VolumeAttributesClass
    *
    * Expected answers: code 200 : VolumeAttributesClass (OK) code 201 :
    * VolumeAttributesClass (Created) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttributesClass
    * @param volumeAttributesClass
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param dryRun
    *   When present, indicates that modifications should not be persisted. An
    *   invalid or unrecognized dryRun directive will result in an error
    *   response and no further processing of the request. Valid values are: -
    *   All: all dry run stages will be processed
    * @param fieldManager
    *   fieldManager is a name associated with the actor or entity that is
    *   making these changes. The value must be less than or 128 characters
    *   long, and only contain printable characters, as defined by
    *   https://golang.org/pkg/unicode/#IsPrint.
    * @param fieldValidation
    *   fieldValidation instructs the server on how to handle objects in the
    *   request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid
    *   values are: - Ignore: This will ignore any unknown fields that are
    *   silently dropped from the object, and will ignore all but the last
    *   duplicate field that the decoder encounters. This is the default
    *   behavior prior to v1.23. - Warn: This will send a warning via the
    *   standard warning response header for each unknown field that is dropped
    *   from the object, and for each duplicate field that is encountered. The
    *   request will still succeed if there are no other errors, and will only
    *   persist the last of any duplicate fields. This is the default in v1.23+ -
    *   Strict: This will fail the request with a BadRequest error if any
    *   unknown fields would be dropped from the object, or if any duplicate
    *   fields are present. The error returned from the server will contain all
    *   unknown and duplicate fields encountered.
    */
  def replaceStorageV1VolumeAttributesClass(
      name: String,
      volumeAttributesClass: VolumeAttributesClass,
      pretty: Option[String] = scala.None,
      dryRun: Option[String] = scala.None,
      fieldManager: Option[String] = scala.None,
      fieldValidation: Option[String] = scala.None
  ): sttp.client4.Request[
    Either[ResponseException[String], VolumeAttributesClass]
  ] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/volumeattributesclasses/${namePathParam}"
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("dryRun", dryRun, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldManager",
            fieldManager,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldValidation",
            fieldValidation,
            FormStyleFormat.FORM,
            true
          ): _*
        )

    basicRequest
      .method(Method.PUT, requestURL)
      .contentType("application/json")
      .body(asJson(volumeAttributesClass))
      .response(asJson[VolumeAttributesClass])

  /** watch changes to an object of kind CSIDriver. deprecated: use the 'watch'
    * parameter with a list operation instead, filtered to a single item with
    * the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSIDriver
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1CSIDriver(
      name: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/csidrivers/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of CSIDriver. deprecated: use the
    * 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1CSIDriverList(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/csidrivers"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch changes to an object of kind CSINode. deprecated: use the 'watch'
    * parameter with a list operation instead, filtered to a single item with
    * the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSINode
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1CSINode(
      name: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/csinodes/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of CSINode. deprecated: use the 'watch'
    * parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1CSINodeList(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/csinodes"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of CSIStorageCapacity. deprecated: use
    * the 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1CSIStorageCapacityListForAllNamespaces(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/csistoragecapacities"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch changes to an object of kind CSIStorageCapacity. deprecated: use the
    * 'watch' parameter with a list operation instead, filtered to a single item
    * with the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the CSIStorageCapacity
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1NamespacedCSIStorageCapacity(
      name: String,
      namespace: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/namespaces/${namespacePathParam}/csistoragecapacities/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of CSIStorageCapacity. deprecated: use
    * the 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param namespace
    *   object name and auth scope, such as for teams and projects
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1NamespacedCSIStorageCapacityList(
      namespace: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namespacePathParam = PathSerializable.serialize(
      "namespace",
      namespace,
      PathStyleFormat.SIMPLE,
      false
    )
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/namespaces/${namespacePathParam}/csistoragecapacities"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch changes to an object of kind StorageClass. deprecated: use the
    * 'watch' parameter with a list operation instead, filtered to a single item
    * with the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the StorageClass
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1StorageClass(
      name: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/storageclasses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of StorageClass. deprecated: use the
    * 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1StorageClassList(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/storageclasses"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch changes to an object of kind VolumeAttachment. deprecated: use the
    * 'watch' parameter with a list operation instead, filtered to a single item
    * with the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttachment
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1VolumeAttachment(
      name: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/volumeattachments/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of VolumeAttachment. deprecated: use
    * the 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1VolumeAttachmentList(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/volumeattachments"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch changes to an object of kind VolumeAttributesClass. deprecated: use
    * the 'watch' parameter with a list operation instead, filtered to a single
    * item with the 'fieldSelector' parameter.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param name
    *   name of the VolumeAttributesClass
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1VolumeAttributesClass(
      name: String,
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val namePathParam =
      PathSerializable.serialize("name", name, PathStyleFormat.SIMPLE, false)
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/volumeattributesclasses/${namePathParam}"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

  /** watch individual changes to a list of VolumeAttributesClass. deprecated:
    * use the 'watch' parameter with a list operation instead.
    *
    * Expected answers: code 200 : WatchEvent (OK) code 401 : (Unauthorized)
    *
    * @param allowWatchBookmarks
    *   allowWatchBookmarks requests watch events with type \"BOOKMARK\".
    *   Servers that do not implement bookmarks may ignore this flag and
    *   bookmarks are sent at the server's discretion. Clients should not assume
    *   bookmarks are returned at any specific interval, nor may they assume the
    *   server will send any BOOKMARK event during a session. If this is not a
    *   watch, this field is ignored.
    * @param continue
    *   The continue option should be set when retrieving more results from the
    *   server. Since this value is server defined, clients may only use the
    *   continue value from a previous query result with identical query
    *   parameters (except for the value of continue) and the server may reject
    *   a continue value it does not recognize. If the specified continue value
    *   is no longer valid whether due to expiration (generally five to fifteen
    *   minutes) or a configuration change on the server, the server will
    *   respond with a 410 ResourceExpired error together with a continue token.
    *   If the client needs a consistent list, it must restart their list
    *   without the continue field. Otherwise, the client may send another list
    *   request with the token received with the 410 error, the server will
    *   respond with a list starting from the next key, but from the latest
    *   snapshot, which is inconsistent from the previous list results - objects
    *   that are created, modified, or deleted after the first list request will
    *   be included in the response, as long as their keys are after the \"next
    *   key\". This field is not supported when watch is true. Clients may start
    *   a watch from the last resourceVersion value returned by the server and
    *   not miss any modifications.
    * @param fieldSelector
    *   A selector to restrict the list of returned objects by their fields.
    *   Defaults to everything.
    * @param labelSelector
    *   A selector to restrict the list of returned objects by their labels.
    *   Defaults to everything.
    * @param limit
    *   limit is a maximum number of responses to return for a list call. If
    *   more items exist, the server will set the `continue` field on the list
    *   metadata to a value that can be used with the same initial query to
    *   retrieve the next set of results. Setting a limit may return fewer than
    *   the requested amount of items (up to zero items) in the event all
    *   requested objects are filtered out and clients should only use the
    *   presence of the continue field to determine whether more results are
    *   available. Servers may choose not to support the limit argument and will
    *   return all of the available results. If limit is specified and the
    *   continue field is empty, clients may assume that no more results are
    *   available. This field is not supported if watch is true. The server
    *   guarantees that the objects returned when using continue will be
    *   identical to issuing a single list call without a limit - that is, no
    *   objects created, modified, or deleted after the first request is issued
    *   will be included in any subsequent continued requests. This is sometimes
    *   referred to as a consistent snapshot, and ensures that a client that is
    *   using limit to receive smaller chunks of a very large result can ensure
    *   they see all possible objects. If objects are updated during a chunked
    *   list the version of the object that was present at the time the first
    *   list result was calculated is returned.
    * @param pretty
    *   If 'true', then the output is pretty printed. Defaults to 'false' unless
    *   the user-agent indicates a browser or command-line HTTP tool (curl and
    *   wget).
    * @param resourceVersion
    *   resourceVersion sets a constraint on what resource versions a request
    *   may be served from. See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param resourceVersionMatch
    *   resourceVersionMatch determines how resourceVersion is applied to list
    *   calls. It is highly recommended that resourceVersionMatch be set for
    *   list calls where resourceVersion is set See
    *   https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions
    *   for details. Defaults to unset
    * @param sendInitialEvents
    *   `sendInitialEvents=true` may be set together with `watch=true`. In that
    *   case, the watch stream will begin with synthetic events to produce the
    *   current state of objects in the collection. Once all such events have
    *   been sent, a synthetic \"Bookmark\" event will be sent. The bookmark
    *   will report the ResourceVersion (RV) corresponding to the set of
    *   objects, and be marked with `\"k8s.io/initial-events-end\": \"true\"`
    *   annotation. Afterwards, the watch stream will proceed as usual, sending
    *   watch events corresponding to changes (subsequent to the RV) to objects
    *   watched. When `sendInitialEvents` option is set, we require
    *   `resourceVersionMatch` option to also be set. The semantic of the watch
    *   request is as following: - `resourceVersionMatch` = NotOlderThan is
    *   interpreted as \"data at least as new as the provided
    *   `resourceVersion`\" and the bookmark event is send when the state is
    *   synced to a `resourceVersion` at least as fresh as the one provided by
    *   the ListOptions. If `resourceVersion` is unset, this is interpreted as
    *   \"consistent read\" and the bookmark event is send when the state is
    *   synced at least to the moment when request started being processed. -
    *   `resourceVersionMatch` set to any other value or unset Invalid error is
    *   returned. Defaults to true if `resourceVersion=\"\"` or
    *   `resourceVersion=\"0\"` (for backward compatibility reasons) and to
    *   false otherwise.
    * @param timeoutSeconds
    *   Timeout for the list/watch call. This limits the duration of the call,
    *   regardless of any activity or inactivity.
    * @param watch
    *   Watch for changes to the described resources and return them as a stream
    *   of add, update, and remove notifications. Specify resourceVersion.
    */
  def watchStorageV1VolumeAttributesClassList(
      allowWatchBookmarks: Option[Boolean] = scala.None,
      continue: Option[String] = scala.None,
      fieldSelector: Option[String] = scala.None,
      labelSelector: Option[String] = scala.None,
      limit: Option[Int] = scala.None,
      pretty: Option[String] = scala.None,
      resourceVersion: Option[String] = scala.None,
      resourceVersionMatch: Option[String] = scala.None,
      sendInitialEvents: Option[Boolean] = scala.None,
      timeoutSeconds: Option[Int] = scala.None,
      watch: Option[Boolean] = scala.None
  ): sttp.client4.Request[Either[ResponseException[String], WatchEvent]] =
    val requestURL =
      uri"$baseUrl/apis/storage.k8s.io/v1/watch/volumeattributesclasses"
        .addParams(
          FormSerializable.serialize(
            "allowWatchBookmarks",
            allowWatchBookmarks,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "continue",
            continue,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "fieldSelector",
            fieldSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "labelSelector",
            labelSelector,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("limit", limit, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable
            .serialize("pretty", pretty, FormStyleFormat.FORM, true): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersion",
            resourceVersion,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "resourceVersionMatch",
            resourceVersionMatch,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "sendInitialEvents",
            sendInitialEvents,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable.serialize(
            "timeoutSeconds",
            timeoutSeconds,
            FormStyleFormat.FORM,
            true
          ): _*
        )
        .addParams(
          FormSerializable
            .serialize("watch", watch, FormStyleFormat.FORM, true): _*
        )

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[WatchEvent])

end StorageV1Api
