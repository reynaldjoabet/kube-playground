/** Kubernetes No description provided (generated by Openapi Generator
  * https://github.com/openapitools/openapi-generator)
  *
  * The version of the OpenAPI document: 1.34
  *
  * NOTE: This class is auto generated by OpenAPI Generator
  * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
  * the class manually.
  */
package kubescala.coordination.api

import kubescala.coordination.models.APIGroup
import kubescala.coordination.JsonSupport.{*, given}
import kubescala.coordination.FormSerializable
import kubescala.coordination.FormStyleFormat
import kubescala.coordination.HeaderSerializable
import kubescala.coordination.ApiKeyLocation
import kubescala.coordination.PathStyleFormat
import kubescala.coordination.PathSerializable
import kubescala.coordination.CookieSerializable
import kubescala.coordination.Helpers.*
import sttp.client4.jsoniter.*
import sttp.client4.*
import sttp.model.Method

object CoordinationApi:
  def apply(baseUrl: String = "http://localhost"): CoordinationApi[
    kubescala.coordination.Authorization.NoAuthorization.type
  ] = CoordinationApi(
    baseUrl,
    kubescala.coordination.Authorization.NoAuthorization
  )
  def withBasicAuth(
      baseUrl: String,
      username: String,
      password: String
  ): CoordinationApi[kubescala.coordination.Authorization.BasicAuth] =
    CoordinationApi(
      baseUrl,
      kubescala.coordination.Authorization.BasicAuth(username, password)
    )

  def withApiKeyAuth(
      baseUrl: String,
      apiKey: String
  ): CoordinationApi[kubescala.coordination.Authorization.ApiKey] =
    CoordinationApi(
      baseUrl,
      kubescala.coordination.Authorization.ApiKey(apiKey)
    )

  def withBearerTokenAuth(
      baseUrl: String,
      token: String
  ): CoordinationApi[kubescala.coordination.Authorization.BearerToken] =
    CoordinationApi(
      baseUrl,
      kubescala.coordination.Authorization.BearerToken(token)
    )

case class CoordinationApi[
    Auth <: kubescala.coordination.Authorization
] private (baseUrl: String, authConfig: kubescala.coordination.Authorization):
  def withBasicAuth(
      username: String,
      password: String
  ): CoordinationApi[kubescala.coordination.Authorization.BasicAuth] =
    copy(authConfig =
      kubescala.coordination.Authorization.BasicAuth(username, password)
    )

  def withApiKeyAuth(
      apiKey: String
  ): CoordinationApi[kubescala.coordination.Authorization.ApiKey] =
    copy(authConfig = kubescala.coordination.Authorization.ApiKey(apiKey))

  def withNoAuth: CoordinationApi[
    kubescala.coordination.Authorization.NoAuthorization.type
  ] =
    copy(authConfig = kubescala.coordination.Authorization.NoAuthorization)

  def withBearerTokenAuth(
      token: String
  ): CoordinationApi[kubescala.coordination.Authorization.BearerToken] =
    copy(authConfig = kubescala.coordination.Authorization.BearerToken(token))

  /** get information of a group
    *
    * Expected answers: code 200 : APIGroup (OK) code 401 : (Unauthorized)
    */
  def getCoordinationAPIGroup
      : sttp.client4.Request[Either[ResponseException[String], APIGroup]] =
    val requestURL =
      uri"$baseUrl/apis/coordination.k8s.io/"

    basicRequest
      .method(Method.GET, requestURL)
      .contentType("application/json")
      .response(asJson[APIGroup])

end CoordinationApi
